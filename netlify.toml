# Build y publicación del frontend (monorepo)
[build]
  command = "npm run build"
  publish = "frontend/build"

# SPA: todas las rutas a index.html (ya está en frontend/public/_redirects)
# [[redirects]]
#   from = "/*"
#   to = "/index.html"
#   status = 200
