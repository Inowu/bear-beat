# Build del frontend (monorepo)
[build]
  base = "frontend"
  command = "npm run build"
  publish = "build"

# SPA: todas las rutas deben servir index.html (evitar 404 en recargas y enlaces directos)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
