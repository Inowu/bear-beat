node_modules
.DS_Store
**/.DS_Store
list-ftp.sh
ftp-stats.py
# Local Netlify folder
.netlify
# Playwright CLI artifacts
.playwright-cli
output
# Local audit screenshots (may contain PII depending on DB; keep untracked)
audit/screenshots
# Production audit artifacts (read-only, may include masked screenshots; keep untracked)
audit-artifacts
