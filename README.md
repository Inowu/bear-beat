# Bear Beat

- **Documentación completa:** ver carpeta [docs/](docs/) (cambios implementados, deploy, Facebook, etc.).

## Running backend

---

- Start the database
  `docker compose up -d db`

- Run the API (Needs the .env file to run correctly)
  `npm run dev`

# Running both projects

---

To run both projects run this command in the root folder that contains the backend and frontend folders:
`npm run start`
