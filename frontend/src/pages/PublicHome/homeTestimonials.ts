export type HomeTestimonial = {
  id: string;
  title: string;
  metric?: string;
  quote: string;
};

/**
 * TODO: Add real testimonials with explicit permission (type of DJ + city + short quote).
 * IMPORTANT: Do not add invented names/photos. Keep this list empty until real data exists.
 */
export const HOME_TESTIMONIALS: HomeTestimonial[] = [];

